name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: unzip
      run: sh install/install_github.sh
    - name: z3
      run: sh install/install_z3.sh


