name: DESCAM-BUILD

on: [push, pull_request]

jobs:
  build:

    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-16.04, ubuntu-18.04]
    env:
      SCAM_HOME: "${GITHUB_WORKSPACE}"
      CMAKE_BIN: "cmake"
      PYTHON3: "python3"
    steps:
      - uses: actions/checkout@v1
      - name: Build System Generation
        run: mkdir build && cd ./build && $CMAKE_BIN ../
      - name: Build SYSTEMC
        run: $CMAKE_BIN --build ./build/ --target SYSTEMC -- -j4