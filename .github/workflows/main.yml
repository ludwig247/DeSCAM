name: C/C++ CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
    - uses: ./.github/actions/main
    - name: test 
      run: pwd
    - name: install
      run: sh install/install_gitub.sh
